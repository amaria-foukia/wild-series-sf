{% extends 'base.html.twig' %}

{% block title %}Add Category Form {% endblock %}

{% block body %}

    <h1>Ajouter une catégorie </h1>

    <section style="width: 50%">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-danger" type="submit">Add a category</button>
        {{ form_end(form) }}
    </section>

{% endblock %}
